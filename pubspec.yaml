name: music_player_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  just_audio: ^0.9.42
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  palette_generator: ^0.3.3+7
  audioplayers: ^6.5.1
  shared_preferences: ^2.5.3
  flutter_svg: ^2.2.4

  on_audio_query: ^2.9.0

  permission_handler: ^12.0.1
  file_picker: ^10.3.10
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: logo/hmmmmmmmmmmmm.png


flutter:
  uses-material-design: true
  assets:
    - images/
    - assets/audios/